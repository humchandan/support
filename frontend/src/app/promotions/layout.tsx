import React from 'react';

export const metadata = {
  title: 'Aries Promotions — Campaign & Rewards Hub',
  description: 'Explore dynamic campaigns, reward events, validator incentives, and staking milestones launched across the Aries sovereign blockchain ecosystem.',
};

export default function PromotionsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
