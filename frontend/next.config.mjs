/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow your mobile device's local network IP to access the dev server
  allowedDevOrigins: ['192.168.29.35'],

  /* other config options here */
};

export default nextConfig;